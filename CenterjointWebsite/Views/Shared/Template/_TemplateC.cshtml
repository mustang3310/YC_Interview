@{
    IList<string> texts = Model.Texts;
    IList<string> images = Model.Images;
}
<div class="container">
    <div class="pt-3 image-container position-relative">
        <img src="@images[0]" class="img-fluid image-block" alt="" />
    </div>
</div>